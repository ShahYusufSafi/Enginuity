import React from "react";

/**
 * @deprecated Unused and unwired. Rebuild against real project/run data when
 * persistence exists; there is nothing to search until then.
 */
export const SearchBar: React.FC = () => {
  return (
    <div>
      <input placeholder="Search" />
    </div>
  );
};
